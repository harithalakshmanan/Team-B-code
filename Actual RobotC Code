#pragma config(Motor,  port1,           rightBack,     tmotorVex393_HBridge, openLoop)
#pragma config(Motor,  port2,           rightMiddle,   tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port3,           leftBack,      tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port4,           leftMiddle,    tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port5,           rightArm,      tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port6,           leftArm,       tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port7,           Claw,          tmotorVex393HighSpeed_MC29, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#include "drive.h"

task main()
{

	//declare constants
	int CLAW_SPEED = 127;
	int variable = 10;

	//create variables
	int leftSpeed = 0;
	int rightSpeed = 0;
	int armSpeed = 0;
	int openClaw = 0;
	int closeClaw = 0;


	//drive

	while(true)
	{
		//set variables
		leftSpeed = vexRT[Ch3];
		rightSpeed = vexRT[Ch2];


		if (abs(leftSpeed) < variable)
		{
			leftSpeed = 0;
		}


		if (abs(rightSpeed) < variable)
		{
			rightSpeed = 0;
		}

		//Represents code to drive robot

		//Have to enter code to move the arm, not completed yet
		if (abs(armSpeed) < variable)
		{
			armSpeed = 0;
		}

		//have to enter code to move the claw, not completed yet
